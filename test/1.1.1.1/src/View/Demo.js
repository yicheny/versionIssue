import React from 'react';
import {data} from "../Data/data";

function Demo(props) {
    console.log(data);
    return (
        <div>
            treeTable
        </div>
    );
}

export default Demo;